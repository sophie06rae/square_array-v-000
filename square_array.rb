def square_array(array)
  # your code here
  array.each do |number| # do begins a block
    # the lines between the do/end are the block's body
    puts "#{number}"
  end # end terminates the block
end
